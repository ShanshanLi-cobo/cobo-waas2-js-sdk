/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BridgingFee from './BridgingFee';

/**
 * The PaymentPayoutItem model module.
 * @module model/PaymentPayoutItem
 */
class PaymentPayoutItem {
    /**
     * Constructs a new <code>PaymentPayoutItem</code>.
     * @alias module:model/PaymentPayoutItem
     * @param token_id {String} The token ID of the payout item.
     * @param amount {String} The amount of the payout item. 
     */
    constructor(token_id, amount) { 
        
        PaymentPayoutItem.initialize(this, token_id, amount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, token_id, amount) { 
        obj['token_id'] = token_id;
        obj['amount'] = amount;
    }

    /**
     * Constructs a <code>PaymentPayoutItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentPayoutItem} obj Optional instance to populate.
     * @return {module:model/PaymentPayoutItem} The populated <code>PaymentPayoutItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentPayoutItem();

            if (data.hasOwnProperty('token_id')) {
                obj['token_id'] = ApiClient.convertToType(data['token_id'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('bridging_fee')) {
                obj['bridging_fee'] = BridgingFee.constructFromObject(data['bridging_fee']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentPayoutItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentPayoutItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentPayoutItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['token_id'] && !(typeof data['token_id'] === 'string' || data['token_id'] instanceof String)) {
            throw new Error("Expected the field `token_id` to be a primitive type in the JSON string but got " + data['token_id']);
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // validate the optional field `bridging_fee`
        if (data['bridging_fee']) { // data not null
          if (!!BridgingFee.validateJSON) {
            BridgingFee.validateJSON(data['bridging_fee']);
          }
        }

        return true;
    }


}

PaymentPayoutItem.RequiredProperties = ["token_id", "amount"];

/**
 * The token ID of the payout item.
 * @member {String} token_id
 */
PaymentPayoutItem.prototype['token_id'] = undefined;

/**
 * The amount of the payout item. 
 * @member {String} amount
 */
PaymentPayoutItem.prototype['amount'] = undefined;

/**
 * @member {module:model/BridgingFee} bridging_fee
 */
PaymentPayoutItem.prototype['bridging_fee'] = undefined;






export default PaymentPayoutItem;

