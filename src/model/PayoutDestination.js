/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PayoutFeeData from './PayoutFeeData';

/**
 * The PayoutDestination model module.
 * @module model/PayoutDestination
 */
class PayoutDestination {
    /**
     * Constructs a new <code>PayoutDestination</code>.
     * @alias module:model/PayoutDestination
     * @param address {String} The destination address.
     * @param amount {String} The amount of cryptocurrency to send to the destination.
     */
    constructor(address, amount) { 
        
        PayoutDestination.initialize(this, address, amount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, address, amount) { 
        obj['address'] = address;
        obj['amount'] = amount;
    }

    /**
     * Constructs a <code>PayoutDestination</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayoutDestination} obj Optional instance to populate.
     * @return {module:model/PayoutDestination} The populated <code>PayoutDestination</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayoutDestination();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('loop_enabled')) {
                obj['loop_enabled'] = ApiClient.convertToType(data['loop_enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('network_fee')) {
                obj['network_fee'] = PayoutFeeData.constructFromObject(data['network_fee']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayoutDestination</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayoutDestination</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayoutDestination.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }
        // validate the optional field `network_fee`
        if (data['network_fee']) { // data not null
          if (!!PayoutFeeData.validateJSON) {
            PayoutFeeData.validateJSON(data['network_fee']);
          }
        }

        return true;
    }


}

PayoutDestination.RequiredProperties = ["address", "amount"];

/**
 * The destination address.
 * @member {String} address
 */
PayoutDestination.prototype['address'] = undefined;

/**
 * The amount of cryptocurrency to send to the destination.
 * @member {String} amount
 */
PayoutDestination.prototype['amount'] = undefined;

/**
 * The email of the destination.
 * @member {String} email
 */
PayoutDestination.prototype['email'] = undefined;

/**
 * The note of the destination.
 * @member {String} note
 */
PayoutDestination.prototype['note'] = undefined;

/**
 * Enable loop mode for standard transfers when source is asset wallet. Only applicable when: - `payout_mode` is `Normal` - `source_type` is asset wallet 
 * @member {Boolean} loop_enabled
 * @default false
 */
PayoutDestination.prototype['loop_enabled'] = false;

/**
 * @member {module:model/PayoutFeeData} network_fee
 */
PayoutDestination.prototype['network_fee'] = undefined;






export default PayoutDestination;

