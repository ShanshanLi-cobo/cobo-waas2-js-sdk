/**
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KyaScreeningResultRiskAssessment from './KyaScreeningResultRiskAssessment';
import KyaScreeningStatus from './KyaScreeningStatus';

/**
 * The KyaScreeningResult model module.
 * @module model/KyaScreeningResult
 */
class KyaScreeningResult {
    /**
     * Constructs a new <code>KyaScreeningResult</code>.
     * Complete address screening result including risk assessment.
     * @alias module:model/KyaScreeningResult
     * @param request_id {String} The idempotency identifier from the request, unique within your organization, used for tracking and troubleshooting. Only present in create response.
     * @param screening_id {String} The unique system-generated identifier for this screening request (UUID format, fixed 36 characters).
     * @param address {String} The screened blockchain address.
     * @param chain_id {String} The chain identifier.
     * @param created_timestamp {Number} The time when the screening request was created, in Unix timestamp format, measured in milliseconds.
     * @param requested_by {String} The identifier of the user or application that created this screening request.
     * @param status {module:model/KyaScreeningStatus} 
     */
    constructor(request_id, screening_id, address, chain_id, created_timestamp, requested_by, status) { 
        
        KyaScreeningResult.initialize(this, request_id, screening_id, address, chain_id, created_timestamp, requested_by, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, request_id, screening_id, address, chain_id, created_timestamp, requested_by, status) { 
        obj['request_id'] = request_id;
        obj['screening_id'] = screening_id;
        obj['address'] = address;
        obj['chain_id'] = chain_id;
        obj['created_timestamp'] = created_timestamp;
        obj['requested_by'] = requested_by;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>KyaScreeningResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KyaScreeningResult} obj Optional instance to populate.
     * @return {module:model/KyaScreeningResult} The populated <code>KyaScreeningResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KyaScreeningResult();

            if (data.hasOwnProperty('request_id')) {
                obj['request_id'] = ApiClient.convertToType(data['request_id'], 'String');
            }
            if (data.hasOwnProperty('screening_id')) {
                obj['screening_id'] = ApiClient.convertToType(data['screening_id'], 'String');
            }
            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('created_timestamp')) {
                obj['created_timestamp'] = ApiClient.convertToType(data['created_timestamp'], 'Number');
            }
            if (data.hasOwnProperty('requested_by')) {
                obj['requested_by'] = ApiClient.convertToType(data['requested_by'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = KyaScreeningStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('risk_assessment')) {
                obj['risk_assessment'] = KyaScreeningResultRiskAssessment.constructFromObject(data['risk_assessment']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KyaScreeningResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KyaScreeningResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KyaScreeningResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['request_id'] && !(typeof data['request_id'] === 'string' || data['request_id'] instanceof String)) {
            throw new Error("Expected the field `request_id` to be a primitive type in the JSON string but got " + data['request_id']);
        }
        // ensure the json data is a string
        if (data['screening_id'] && !(typeof data['screening_id'] === 'string' || data['screening_id'] instanceof String)) {
            throw new Error("Expected the field `screening_id` to be a primitive type in the JSON string but got " + data['screening_id']);
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }
        // ensure the json data is a string
        if (data['requested_by'] && !(typeof data['requested_by'] === 'string' || data['requested_by'] instanceof String)) {
            throw new Error("Expected the field `requested_by` to be a primitive type in the JSON string but got " + data['requested_by']);
        }
        // validate the optional field `risk_assessment`
        if (data['risk_assessment']) { // data not null
          if (!!KyaScreeningResultRiskAssessment.validateJSON) {
            KyaScreeningResultRiskAssessment.validateJSON(data['risk_assessment']);
          }
        }

        return true;
    }


}

KyaScreeningResult.RequiredProperties = ["request_id", "screening_id", "address", "chain_id", "created_timestamp", "requested_by", "status"];

/**
 * The idempotency identifier from the request, unique within your organization, used for tracking and troubleshooting. Only present in create response.
 * @member {String} request_id
 */
KyaScreeningResult.prototype['request_id'] = undefined;

/**
 * The unique system-generated identifier for this screening request (UUID format, fixed 36 characters).
 * @member {String} screening_id
 */
KyaScreeningResult.prototype['screening_id'] = undefined;

/**
 * The screened blockchain address.
 * @member {String} address
 */
KyaScreeningResult.prototype['address'] = undefined;

/**
 * The chain identifier.
 * @member {String} chain_id
 */
KyaScreeningResult.prototype['chain_id'] = undefined;

/**
 * Optional note for this address screening.
 * @member {String} note
 */
KyaScreeningResult.prototype['note'] = undefined;

/**
 * The time when the screening request was created, in Unix timestamp format, measured in milliseconds.
 * @member {Number} created_timestamp
 */
KyaScreeningResult.prototype['created_timestamp'] = undefined;

/**
 * The identifier of the user or application that created this screening request.
 * @member {String} requested_by
 */
KyaScreeningResult.prototype['requested_by'] = undefined;

/**
 * @member {module:model/KyaScreeningStatus} status
 */
KyaScreeningResult.prototype['status'] = undefined;

/**
 * @member {module:model/KyaScreeningResultRiskAssessment} risk_assessment
 */
KyaScreeningResult.prototype['risk_assessment'] = undefined;






export default KyaScreeningResult;

